# bullockcart_loadcheck
A simple Python program that checks if a bullock cart can carry given loads. If a load exceeds the cart capacity, it removes the load and continues checking the remaining loads.
